# Never commit a real token. Copy this file to .env only for tools that load it.
# asana-cli itself intentionally reads the process environment and does not load .env.
ASANA_ACCESS_TOKEN=replace-with-your-personal-access-token
